{
  "mcpServers": {
    "wandb": {
      "command": "python",
      "args": ["-m", "mcp_wandb_server.server"],
      "cwd": "${PROJECT_ROOT}",
      "env": {
        "WANDB_API_KEY": "${WANDB_API_KEY}"
      }
    }
  }
}

