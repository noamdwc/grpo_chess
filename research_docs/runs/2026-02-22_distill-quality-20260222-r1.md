## Run Report: distill-quality-20260222-r1

- Date: 2026-02-22
- Job name: `distill-quality-20260222-r1`
- Machine: `L4`
- Final status: `Failed`
- Branch: `feature/lightning_training`
- Commit: `e73ca8d`
- Plan doc: `research_docs/experiments/2026-02-22_distill-quality-mode-two-paths.md`
- Config path (generated in-job): `/tmp/distill_labelsafe.lightning.yaml`

### Submission Command

```bash
set -e
cd /teamspace/studios/this_studio
rm -rf grpo_chess_run || true
git clone --branch feature/lightning_training --single-branch git@github.com:noamdwc/grpo_chess.git grpo_chess_run || git clone --branch feature/lightning_training --single-branch https://github.com/noamdwc/grpo_chess.git grpo_chess_run
cd grpo_chess_run
git checkout e73ca8d
git submodule update --init --recursive
python -m pip install -r requirements.txt
PINNED_COMMIT=e73ca8d PRETRAIN_CKPT_DRIVE_URL="https://drive.google.com/file/d/1qLx8a18HD7d3QswvKI9iVe8ujMj0eXGu/view?usp=sharing" DISTILL_MODE=quality PERSIST_NAMESPACE=distill_labelsafe_quality_v1 PERSIST_BACKEND=gdrive GDRIVE_FOLDER_ID=1UXrGzc5uDzqJDwkRqdXNn0RlE0gAi-Pq GDRIVE_PERSIST_REQUIRED=1 GDRIVE_SYNC_SHARDS=1 GDRIVE_DOWNLOAD_MAX_SHARDS=8 GDRIVE_UPLOAD_MAX_SHARDS=8 DISTILL_MAX_SHARDS=8 DISTILL_FORCE_REBUILD_DATA=0 TEACHER_MAX_SAMPLES=300000 bash scripts/lightning/run_distill_labelsafe.sh
```

### Failure

- Failed before data generation/training.
- Root cause:
  - Google Drive credential env vars were not available in the job runtime.
  - Runner was submitted with `GDRIVE_PERSIST_REQUIRED=1`, so initial sync hard-failed.
- Error:
  - `No Google Drive service-account credential found. Set GDRIVE_SERVICE_ACCOUNT_FILE or GDRIVE_SERVICE_ACCOUNT_JSON or GDRIVE_SERVICE_ACCOUNT_JSON_B64.`

### Notes

- This run uses `DISTILL_MODE=quality` to generate teacher-label shards (`src.distill.generate_dataset`) instead of DeepMind bag conversion.
- Quality preflight checks are enabled via the updated cloud runner and will fail early if labels are too sparse.
- Persistence backend is Google Drive (`artifacts` folder) so shard and metadata cache can be reused.
- In this MCP environment, logs are not available while a job is running; logs can be pulled after completion/failure.
